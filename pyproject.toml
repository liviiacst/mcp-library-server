[project]
name = "mcp-library-server"
version = "0.1.0"
description = "My first Model Context Protocol server built with Python."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "mcp==1.28.1"
]

[tool.setuptools.packages.find]
where = ["src"]